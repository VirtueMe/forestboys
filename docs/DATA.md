# DATA

Sanity schema, GROQ queries, and IndexedDB caching strategy.

## Status

`draft`

---

<!-- Document the Sanity project/dataset config, all GROQ queries (Query A lean / Query B detail / per-entity queries), IDB schema (DB_NAME, DB_VERSION, CACHE_KEY), and the cache invalidation rules. -->
