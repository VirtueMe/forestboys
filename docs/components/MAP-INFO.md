# MAP-INFO

`MapInfo.vue` — MapLibre GL JS map with clustering, colour-coded markers, and detail panel integration.

## Status

`todo`

---

<!-- Document the MapLibre setup (CARTO tiles, initial viewport), GeoJSON layer structure, cluster configuration, marker colour derivation (org/district), click handler and slug emission, geolocation dot, and the AppMap/AppDrawer integration pattern. -->
