# CUSTOM-SELECT

`CustomSelect.vue` — Reusable dropdown for org (single-select) and district (multi-select) filters.

## Status

`draft`

---

<!-- Document props (options, modelValue, placeholder, multiple, disabled, colorMap), trigger label logic (single value / "N valgt"), colour dot rendering, click-outside close behaviour, and usage in EventsView. -->
