# EVENT-INFO

`EventInfo.vue` — Original event detail component from milorg-2. Reference only — not used in the rebuild.

## Status

`reference`

---

<!-- Document what the original EventInfo rendered (fields, layout, Bootstrap components used) as a reference for building EventPanel.vue. No implementation target — the rebuild uses EventPanel.vue instead. -->
