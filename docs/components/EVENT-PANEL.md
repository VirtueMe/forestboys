# EVENT-PANEL

`EventPanel.vue` — Event detail renderer used inside the events view.

## Status

`draft`

---

<!-- Document the fields rendered (title, date, org, district, description via blocksToHtml, people, locations, transport, gallery, links), prop shape (IdbEventDetail), and internal layout. -->
