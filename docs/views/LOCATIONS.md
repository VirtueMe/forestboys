# LOCATIONS

`/locations/:slug?` — MapLibre map explorer with location detail panel.

## Status

`todo`

---

<!-- Document the map layout, clustering behaviour, org/district filter integration, colour coding, detail panel (title, description, gallery, linked events/people/stations), and URL slug routing. -->
